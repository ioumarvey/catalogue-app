{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.product_delete'|trans({'%id%': task.id|default('')}) }}
{% endblock %}

{% block body %}
    {% include 'default/_form.html.twig' with {
        page_title: 'action.delete'|trans({'%id%': task.id|default('')}),
        form: form,
        submit_label: 'action.delete'|trans,
        back_to_list_path: 'task_index',
    } %}
{% endblock %}