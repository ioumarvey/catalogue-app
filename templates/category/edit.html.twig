{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.category_edit'|trans({'%id%': category.id|default('')}) }}
{% endblock %}

{% block body %}

    {% include 'default/_form.html.twig' with {
        page_title: 'action.edit'|trans({'%id%': category.id|default('')}),
        form: form,
        submit_label: 'action.edit'|trans,
        back_to_list_path: 'category_index',
    } %}
{% endblock %}
