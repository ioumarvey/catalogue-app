{% include 'default/_form.html.twig' with {
    page_title: 'title.register'|trans,
    attr: {
        method: 'POST',
        action: url('app_register')
    },
    form: form,
    submit_label: 'label.register'|trans,
    back_to_list_path: 'app_login',
} %}