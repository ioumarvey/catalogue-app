{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.tag_delete'|trans({'%id%': tag.id|default('')}) }}
{% endblock %}

{% block body %}
    {% include 'default/_form.html.twig' with {
        page_title: 'action.delete'|trans({'%id%': tag.id|default('')}),
        form: form,
        submit_label: 'action.delete'|trans,
        back_to_list_path: 'tag_index',
    } %}
{% endblock %}